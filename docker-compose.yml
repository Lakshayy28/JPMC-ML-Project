services:
  fri-api-engine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fri-api-engine
    ports:
      - "8000:8000"
    volumes:
      - ./artifacts:/app/artifacts
    restart: unless-stopped